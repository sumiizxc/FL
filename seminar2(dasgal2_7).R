#seminar2 dasgal 2.7
data = c("male", "female", "male")
factor(
    x = data,
    levels = c("male", "female")
)
factor(data) #hervee levels parametriig ashiglahgui  bol tsagaan tolgoin usgeer levelee togtoodog






data = c(1, 2, 1)
factor(
  x = data, 
  levels = c(1,2),
  labels = c("Female", "Male"),
  ordered =TRUE
)

exists("x") #ened x gedeg object bn uu ? 

